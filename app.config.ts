export default defineAppConfig({
  ui: {
    colors: {
      primary: "emerald",
      neutral: "slate",
    },

    button: {
      defaultVariants: {
        color: "primary", // set rõ để dễ test
        size: "lg",
      },
    },
  },
});
